'use strict'

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    //width: width,
    backgroundColor: '#f60',
    alignItems: 'center',
    justifyContent: 'center',
    //color: '#666'
  }
})
