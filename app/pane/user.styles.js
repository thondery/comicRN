'use strict'

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
    //width: width,
    backgroundColor: '#e63',
    alignItems: 'center',
    justifyContent: 'center',
    //color: '#666'
  }
})
